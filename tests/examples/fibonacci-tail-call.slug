var {*} = import(
    "slug.test"
)

//
// Fibonacci sequence calculation using tail call recursion
//

var fib = fn(n, a = 0, b = 1) match {
	[0, ...] => a
	[1, ...] => b
	_        => recur(n - 1, b, a + b)
}

fib(10) /> assertEqual(55, "Fibonacci 10 equals 55")
