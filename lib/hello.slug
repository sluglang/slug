/**
 * A very simple `Hello Slug` tool example
 *
 * Usage:
 * ```bash
 * slug hello [name]
 * ```
 */

@main var sayHi = fn(args = argv()) match {
    [] => println("Hello Slug!")
    [n, ...] => println("Hello {{n}}!")
}
