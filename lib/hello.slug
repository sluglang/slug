/**
 * A very simple `Hello Slug` tool example
 *
 * Usage:
 * ```bash
 * slug hello [name]
 * ```
 */

var sayHi = fn(args) match {
    [] => println("Hello Slug!")
    [n, ...] => println("Hello {{n}}!")
}

sayHi(argv())
